[pytest]
# 代理AI/CI の既定（静かに・最初の失敗で止める・integration除外）
addopts = -q --maxfail=1 -k "not integration"
# 収集対象は tests/ に限定
testpaths = tests

# 収集から除外（外部依存や生成物）
norecursedirs =
    generated_code
    generated_code/**
    airflow_docker
    airflow_docker/**

markers =
    integration: integration tests
