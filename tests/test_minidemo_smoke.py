def test_minidemo_offline_smoke(stub_collector, stub_strategies, capture_obs, tmp_data_dir):
    """
    ネット・DB なしで最小スモーク:
      - minidemo が完走
      - 出力 JSON が生成
      - 観測ログ（ダミー）が呼ばれている
    """
    # Add your test logic here
    assert True  # Placeholder assertion to avoid test failure
